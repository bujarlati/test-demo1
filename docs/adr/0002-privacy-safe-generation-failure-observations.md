# ADR 0002：使用脱敏失败观测驱动生成纠错

- 状态：Accepted
- 日期：2026-07-22

生成可靠性改进以结构化、不可变的“生成失败观测”为数据源，并按稳定原因码、生成阶段与模型路由聚合失败模式。观测只保存经过脱敏和限长的错误信息及运行元数据，不保存用户原始灵感、完整提示词或正文；完整 AI 跟踪仍是短期本地调试资料，不能作为跨用户学习数据。这样既能统计重复故障、验证纠错规则是否有效，也避免把一个读者的内容泄露或自动注入另一个读者的生成过程。
